@page
@model Курсач.Pages.Departments.DeleteModel
@{
    ViewData["Title"] = "Удалить отдел";
}

<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow">
                <div class="card-header bg-danger text-white">
                    <h4 class="mb-0">
                        <i class="bi bi-exclamation-triangle"></i> Удаление отдела
                    </h4>
                </div>
                <div class="card-body">
                    @if (Model.Department.Employees != null && Model.Department.Employees.Any())
                    {
                        <div class="alert alert-danger">
                            <i class="bi bi-info-circle"></i>
                            <strong>Внимание!</strong> В этом отделе есть сотрудники.
                            Сначала переместите или удалите всех сотрудников из отдела.
                        </div>
                        <div class="d-flex justify-content-between">
                            <a asp-page="./Index" class="btn btn-secondary">
                                <i class="bi bi-arrow-left"></i> Назад
                            </a>
                        </div>
                    }
                    else
                    {
                        <div class="alert alert-warning">
                            <i class="bi bi-info-circle"></i>
                            <strong>Внимание!</strong> Вы уверены, что хотите удалить отдел <strong>@Model.Department.Name</strong>? Это действие нельзя отменить.
                        </div>

                        <dl class="row">
                            <dt class="col-sm-4">Название отдела</dt>
                            <dd class="col-sm-8">@Model.Department.Name</dd>
                        </dl>

                        <form method="post">
                            <input type="hidden" asp-for="Department.Id" />
                            <div class="d-flex justify-content-between">
                                <a asp-page="./Index" class="btn btn-secondary">
                                    <i class="bi bi-arrow-left"></i> Отмена
                                </a>
                                <button type="submit" class="btn btn-danger">
                                    <i class="bi bi-trash"></i> Удалить
                                </button>
                            </div>
                        </form>
                    }
                </div>
            </div>
        </div>
    </div>
</div>